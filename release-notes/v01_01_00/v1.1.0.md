





1. 字典功能支持，由后端SQL 控制值，只提供一个查询接口给前端

2. json 序列化与反序列化增强对枚举的处理
   - 增加注解`@JsonIgnore` 对枚举常量值的效果
   - 实现注解`@JsonValue` 与注解 `@JsonIgnore` 并存的效果
   
3. 删除了对`shiro` 组件的依赖

   漏删除的历史遗留

4. Excel 导出功能

   支持复杂结构的导出

   自动合并存在list 的多行数据

5. 其他